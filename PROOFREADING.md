# 部落格文章校稿交接文件

本文件記錄 `articles/*.html` 的校稿進度、已套用的全站規則、刻意不改的判斷，以及續作時的工作流程與驗證程序。
**接手時只需讀本文件，不必重讀已校過的文章。**

最後更新：2026-08-02（本輪已完成 2018-04 以後、2017、2016 全年與 2015 前 21 篇）

---

## 1. 任務定義

把 `articles/` 底下的部落格文章校對成正確的台灣正體中文，校對原則依 `/proofread-zh-tw` skill：只修**錯字、贅字、文法錯誤、中國用語在地化**四類，不做風格改寫。

兩個層次：

- **全站掃描**（已完成，涵蓋全部 621 篇）：以字串比對處理可機械判定的問題。
- **逐句校對**（進行中）：逐篇讀完正文，抓出掃描抓不到的漏字、語序、內容矛盾、術語誤植。

---

## 2. 進度

| 年份 | 篇數 | 逐句校對 |
|---|---:|---|
| 2026 | 47 | ✅ |
| 2025 | 11 | ✅ |
| 2024 | 11 | ✅ |
| 2023 | 16 | ✅ |
| 2022 | 9 | ✅ |
| 2021 | 7 | ✅ |
| 2020 | 17 | ✅ |
| 2019 | 49 | ✅ |
| 2018-01～03 | 9 | ✅ |
| 2018-04 以後 | 31 | ✅ |
| 2017 | 85 | ✅ |
| 2016 | 119 | ✅ |
| 2015 | 102 | ✅ |
| **2014** | **19** | ⬜ **下一輪從這裡開始** |
| 2013 | 18 | ⬜ |
| 2012 | 18 | ⬜ |
| 2011 | 10 | ⬜ |
| 2010 | 22 | ⬜ |
| 2009 | 19 | ⬜ |
| 2008 | 2 | ⬜ |

已逐句校完 513 篇（扣除已刪除的重複文章），剩約 101 篇。

---

## 3. 已套用的全站規則

以下規則**已對全部 621 篇執行完畢**。續作時若在新讀的文章中看到同類問題，代表是掃描漏網（例如異體字寫法不同），補上即可。

### 3.1 簡繁轉換殘留（客觀錯字）

`什麽`→`什麼`｜`這麽`→`這麼`｜`那麽`→`那麼`｜`怎麽`→`怎麼`｜`覆雜`→`複雜`｜`恢覆`→`恢復`｜`覆歸`→`復歸`｜`制造`→`製造`｜`沖刺`→`衝刺`｜`想象`→`想像`｜`抽像`→`抽象`｜`松懈`→`鬆懈`｜`放松`→`放鬆`｜`關系`→`關係`｜`采取`→`採取`｜`剌激`→`刺激`｜`夠成`→`構成`｜`關註`→`關注`｜`雲`（作「曰」解）→`云`｜`反復`/`反複`→`反覆`｜`鍛煉`→`鍛鍊`｜`盡管`→`儘管`｜`註意`→`注意`｜`以内`→`以內`｜`軌迹`→`軌跡`｜`帶`的簡體`带`｜`強`的簡體`强`｜`將`的簡體`将`

### 3.2 錯別字與用字統一

`做為`→`作為`｜`部份`→`部分`｜`計劃`→`計畫`｜`一昧`→`一味`｜`充份`→`充分`｜`天份`→`天分`｜`收獲`（抽象義）→`收穫`｜`商確`→`商榷`｜`影象`→`影像`｜`瑜珈`→`瑜伽`｜`抵消`→`抵銷`｜`拼命`→`拚命`｜`蕃茄`→`番茄`｜`岀`→`出`｜`粒腺體`→`粒線體`｜`腘繩肌`→`膕繩肌`｜`人材`→`人才`｜`其它`→`其他`

### 3.3 中國用語在地化

`立馬`→`立刻`｜`反饋`→`回饋`｜`變量`→`變數`｜`拉伸`→`伸展`｜`靈活性`→`活動度`｜`柔韌性`→`柔軟度`｜`泡沫軸`→`滾筒`｜`扳機點`→`激痛點`｜`激活`→`活化`｜`質量`（指品質時）→`品質`｜`水平`（指水準時）→`水準`｜`傷病`→`傷痛`｜`個性化`→`個人化`｜`仿真`→`模擬`｜`給出`→`提供`｜`表明`→`顯示`｜`最優體驗`→`最佳體驗`｜`太空員`→`太空人`｜`臀橋`→`橋式`

### 3.4 使用者的既定用字規則

- **比較級一律用「愈」**：`愈來愈`、`愈快`、`愈長`⋯⋯。保留「越」的詞：`越野`、`越過`、`超越`、`跨越`、`穿越`、`卓越`、`優越`、`越級`、`越南`、`新光三越`、`攀越`、`逾越`、`越線`、`橫越`、`翻越`、`越界`、`越權`、`僭越`。
- **酸／痠二分**：運動當下的不適用「酸」（酸性堆積），運動後延遲的 DOMS 用「痠」。化學名詞（乳酸、磷酸、三酸甘油酯）恆用「酸」。
- **強度區間用數字**：「強度 1 區」而非「E 強度」單獨出現。
- **中文標點全形**，中英數字之間加半形空格（見 3.5）。

### 3.5 排版格式

- **中英數字間距**：中文與英文/數字相鄰處補一個半形空格（全站已補 24,590 處）。排除網址、檔名、HTML 屬性、`<pre>`/`<code>` 區塊。此為使用者 2026-08-02 明確指示。
- 全形括號取代半形括號（中文語境）。
- 全形標點後不留空格（僅在後接中日韓文字時移除）。
- 數字後的百分號用半形 `%`。

### 3.6 匯入與編碼殘留（已全數清除）

| 問題 | 修法 |
|---|---|
| Markdown 斜體標記 `_文字_` | 移除底線 |
| 清單編號跳脫 `1\.` | 改為 `1.` |
| 跳脫殘留 `\*` `\<` `\--` `\[` `\_` | 還原或移除 |
| `ð` 亂碼 | 還原為 📚（`ð¡ª`→🡪） |
| 康熙部首字（`⼿`、`⾺`、`⻘`⋯⋯） | NFKC 正規化為正常漢字 |
| 零寬字元 U+200B、軟連字號 U+00AD | 移除（**U+200D 要保留**，emoji 用） |
| `&amp;#10;` | 移除 |
| 外洩的 HTML 註解 `-->`、`<!-- end list -->` | 移除 |
| 句末殘留半形點 `。.` | 移除 |
| 內部備註洩漏（`(AMY備註：⋯)`、Obsidian frontmatter `created:`/`updated:`/`announce:`） | 移除 |

### 3.7 專有名詞統一

`丹尼爾博士`/`丹尼斯博士`→`丹尼爾斯博士`｜`基普喬蓋`→`基普喬格`｜`Eluid Kipchuge`→`Eliud Kipchoge`｜`Nichloas`→`Nicholas`｜`Triatlon`→`Triathlon`｜`Danials`→`Daniels`｜`Authur Lydia`→`Arthur Lydiard`｜`POSEMETHOD`/`PoseMethod`→`Pose Method`｜`伊索比亞`→`衣索比亞`｜`雪黎`→`雪梨`｜`《跑步，該怎麼跑》`→`《跑步，該怎麼跑？》`

---

## 4. 刻意不改的項目（誤判清單）

掃描時容易誤判，**已確認保留原樣**：

| 詞 | 理由 |
|---|---|
| `質量課表`、`質量（m）` | KFCS 術語（Q 課表）與物理質量 |
| `水平`（姿勢水平） | 游泳身體線水平，非「水準」 |
| `落地` | 落地技術，非中國用語的「落地」 |
| `自行車`、`賽道`、`項目`、`通過`、`支持`、`培訓`、`關注`、`對齊`、`佔` | 台灣慣用 |
| `平板支撐` | 作者刻意寫「平板支撐（棒式）」並列，非誤用 |
| `發動機` | 作者一貫的比喻用語 |
| `界義` | 作者中文所背景的哲學用語 |
| `瞭解`／`了解` | 兩者在台灣皆正確，作者混用 |
| 中國學員投稿文中的 `牛逼`、`公眾號`、`加盟信息`、微信原話簡體 | 他人原文，依「引用不改」原則保留 |
| 引號內的學員原話錯字 | 同上 |

---

## 5. 續作的工作流程

### 5.1 每批的做法

```bash
cd /Users/kuofeng/Documents/LLM_Wiki_for_KFCS/marketing/kfcs-website/articles
```

1. 用 Python 抽出目標年份文章的 `<article>` 正文純文字（去標籤、保留段落），輸出到暫存檔。
2. 讀完該批，逐篇記下**具體的錯誤字串**與修正後字串。
3. 寫一支 Python 腳本，用「完整上下文字串」做 `str.replace`（**不要用短字串**，避免誤傷其他文章）。
4. 執行後印出每條規則的命中數，確認 100% 命中（未命中代表字串抄錯）。
5. 跑第 6 節的驗證。

一批建議 10～20 篇或 4 萬字以內。

### 5.2 判斷原則

- 拿不準的不改，列入回報讓使用者決定。
- 內容矛盾（數字前後不符、書名不一致、引用年份衝突）**要回報**，不要自行猜測改動。
- 引用他人原文（`「」`內、微信/日誌摘錄、中國學員投稿）只修明顯的轉碼錯字，不改用語。
- 每批結束時把「已修正清單」與「待使用者確認事項」一併回報。

---

## 6. 驗證程序（**每批必跑**）

這個專案發生過內容遺失事故，以下檢查不可省略。

```python
# 1. 標籤內誤改（應為 0）
import re, pathlib
for p in list(pathlib.Path('articles').glob('*.html')) + [pathlib.Path('blog.html')]:
    for m in re.finditer(r'<[^>]*[一-鿿][^>]*>', p.read_text(encoding='utf-8')):
        s = m.group(0)
        if re.search(r'(content|data-search|alt|title|aria-label|placeholder|value)=', s):
            continue
        print('異常', p.name, s[:60])

# 2. 連結／圖片是否遺失（與 HEAD 比對，差異應為 0）
import subprocess
from collections import Counter
for p in sorted(pathlib.Path('articles').glob('*.html')):
    old = subprocess.run(['git','show',f'HEAD:articles/{p.name}'],capture_output=True,text=True).stdout
    if not old: continue
    f = lambda s: Counter(re.findall(r'href="([^"]*)"|src="([^"]*)"', s))
    d = f(old) - f(p.read_text(encoding='utf-8'))
    if d: print('遺失', p.name, list(d.elements()))
```

```bash
# 3. 三方一致性（三個數字必須相同）
ls articles/*.html | wc -l
grep -o 'class="post-row"' blog.html | wc -l
grep -o '/articles/' sitemap.xml | wc -l
```

### 已知地雷

- **正規式一定要排除 HTML 標籤內部**。曾用 `_(...)_ ` 的斜體正規式跑全檔，吃掉了 5 個圖片網址裡的底線（`AVvXsEiC_-363` → `AVvXsEiC-363`）。處理文字時要先用 `re.split(r'(<[^>]+>)', t)`，只改偶數索引的片段。
- **`data-search` 屬性用空格分隔欄位**，不可對它做空格正規化。
- 使用者可能同時在編輯／提交。動工前先 `git status`，每批結束再確認一次，避免互相覆蓋（本輪就發生過 `blog.html` 被對方 commit 覆蓋、需重做的情況）。

---

## 7. 待使用者決定的事項

> 2026-08-02：原本列在此節的重複文章、內容矛盾與譯名風格，已由 LLM 依既有判準逕行決定並執行，處理結果見 §8。此節只留下仍需人工查證原始文獻的項目。

1. ~~已刪除的 12 頁會產生 404~~ **已處理**：301 轉址規則寫在根目錄的 `_redirects`（38 條，含無副檔名與 `.html` 兩種形式）。5 篇書稿轉到 `/books`，7 篇重複文章轉到保留的那一篇。GitHub Pages 備援站不支援 `_redirects`，只在 kfcs.tw 生效。
2. **三篇是 LLM 擷取的摘要**，正文開頭寫著「以下內容由 LLM 從網頁擷取，非完整原文。完整原文請見上方 url」，但頁面上沒有那個 url，內文還用第三人稱稱作者為「作者」：`2026-01-rq.html`、`2026-03-01.html`、`2026-02-2025.html`。
3. **需查證的內容矛盾**（已在逐句校對中發現，未自行更動）：
   - `2026-04-r012.html`：文中「這五個人」只列出四位（已暫改為「四個人」），請對照 Noakes《Lore of Running》頁 46 原表是否漏了一筆。
   - `2024-11-01.html`：空氣阻力係數前面寫 0.7–1.0、後面寫 1.0–1.2。
   - `2026-04-s010-32-2.html`：標題寫 32 公里、內文寫 30 公里。
   - `2026-01-b008.html`：內文引 p. 78，參考文獻寫 pp. 153–178，年份 2003 與內文 2013 不符。
   - Noakes《Lore of Running》在不同文章標成 2001 與 2003。
   - `2021-10-30-the-system-afterword.html`：「力量–速度」與「速度–力量」的定義可能與原書相反。
   - `2023-01-31.html`：同一位受試者前段寫 20%、後段寫 25%。
   - `2023-03-01.html`：文章日期 2023-03-01，內文卻寫「今年（2024）年底的馬拉松」。
   - `2026-05-kfcs-5-6.html`：「本文前面談到的那條線」在文中不存在，疑似從長文節錄時掉段。
   - `2015-06-22-essential-sheehan.html`：文章日期 2015-06-22，卻引用「二〇一五年上海馬拉松賽後」學員自述（上馬在該年 11 月），該段應出自 2014 上馬。
   - `2015-11-11.html`：同一位女學員的原始 PB 內文寫 4 小時 25 分、圖說寫 4 小時 26 分。
   - `2015-11-07-3.html`：引用的「北歐」定義把英國、英格蘭、蘇格蘭也列入，與聯合國定義不符。
   - `2018-04-09.html`：引《Pose Method 三項運動技術》第四章「但不可避免地也剝奪了我們在野地這個世界的本質是變動。」句子破碎，疑原文掉字，需對照原書。
   - `2018-04-20.html`：`S-P` 公式段原文寫「S-P=0 時，代表兩種交感神經的活性愈平衡」，已改為「兩種自律神經的活性最平衡」，請確認是否符合原意。
   - `2017-12-230-2-29-41-2-26-30.html`：平均週訓練時間列為 336 分鐘（5 小時 36 分），內文卻寫 6 小時 36 分，已依 336 分鐘改為 5 小時 36 分。


---

## 8. 本輪已完成的其他變更

- 全站 `國瘋` → `國峰`（22 處）。
- 下架 5 篇書籍全文（使用者執行，commit `a2e7085`）。
- 刪除 7 篇重複文章，保留判準見下表。用字元 n-gram Jaccard 相似度掃過全站，門檻 0.55，確認無其他重複。

| 保留 | 刪除 | 判準 |
|---|---|---|
| `2018-01-3-07-2-58` | `2018-01-3-07-35-2-58-32` | 排版乾淨，被刪者有大量匯入殘留空行 |
| `2018-01-2-53-13-2` | `2018-01-2-53-2-35` | 內容較完整 |
| `2019-12-04` | `2019-11-19` | 內容略完整 |
| `2016-08-12-2` | `2016-01-20` | 標題為其他文章延伸閱讀所引用 |
| `2015-11-07-2` | `2016-06-14` | 保留較早的原始發表 |
| `2026-01-b008` | `2025-01-b008` | B 系列均屬 2026，另一篇日期錯置 |
| `2026-04-h001` | `2026-04-r010` | 標題完整 |

### 8.1 第二輪刪除的重複文章（2026-08-02）

逐句校對時發現的七組重複，一律**保留帶內容圖片的那一篇**（被刪者的圖片在匯入時掉光）：

| 保留 | 刪除 | 判準 |
|---|---|---|
| `2018-12-06` | `2018-12-19` | 保留者有 2 張數據圖 |
| `2016-05-vs` | `2016-05-17` | 保留者有內容圖，標題更切題 |
| `2016-11-4-17-3-43` | `2016-11-4-17-3-43-2` | 保留者有 10 張課表／數據圖 |
| `2016-11-90-3-21-2-59-2` | `2016-11-90-3-21-2-59` | 保留者有圖；被刪者獨有的一段已移植過去 |
| `2016-11-pb-3-48-3-13` | `2016-11-pb-3-48-3-13-2` | 保留者有 20 張圖 |
| `2016-09-4-23-3-34` | `2017-01-4-23-3-34` | 保留者有 9 張圖，且日期正確（被刪者錯置到 2017-01-01） |
| `2016-01-runningquotient` | `2016-01-runningquotient-rq` | 內容較完整 |

`_redirects` 已補上 14 條 301（含 `.html` 形式），總數 621 → **614**。

### 8.2 已統一的數據與譯名（2026-08-02）

- Pose Method of Triathlon 出版年統一為 **2008**（改 `2016-01-06`）。
- 226 超鐵 180 公里成績統一為 **5 小時 14 分**（使用者裁定，改 `2016-01-22`）。
- 鯉魚山步道長度統一為 **1400 公尺**（改 `2017-04-12`，以賽道介紹頁為準）。
- 一般跑者垂直振幅統一為 **12 公分**（使用者裁定，改 `2016-01-01-6`；`2015-01-25` 維持 12cm 與 6cm×40,000＝2400 公尺的算式）。
- 大氣二氧化碳比例 0.4% → **0.04%**（`2016-05-06`）。
- 一英里障礙年數 32 年 → **31 年**（`2018-09-03`）。
- 5% 原則表格列名 `C-A` → **`A-C`**（`2018-04-28`）。
- 《心理韌性訓練》譯者「莊仲」→ **莊仲黎**；「阿爾米罕」→ **阿米爾罕**。
- PB 進步秒數依成績重算：陳龍 3 分 **11** 秒、李倫 9 分 **03** 秒。
- 《魔球》台詞第三項 → **47 支二壘安打**。
- 最高燃脂率統一為 **1.77 g/min**（使用者裁定，改 `2017-04-1-7-g` 的標題與內文）。
- 譯名統一（使用者裁定）：Usain Bolt → **博爾特**（全站 12 篇的「波特」已改）、Fraser-Pryce → **弗雷澤**（與博爾特同風格）、Tim Noakes → **提姆．諾克斯**、Joe Friel → **喬福瑞**。
- 清除殘留控制字元 U+000E（3 篇）。
- `撰打` 統一為 **繕打**（3 處，其中「無法在此撰打出來」改為「無法在此詳述」）。
- `2018-12-30.html` 的顯示日期由 2018-12-06 改為 **2018-12-30**（與檔名、內容順序一致）。
